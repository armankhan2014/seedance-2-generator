{
  "buildCommand": "npx prisma generate && npx next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/sweep-stuck-creations",
      "schedule": "0 * * * *"
    }
  ]
}
